const config = {
  port: process.env.PORT || 3000,
  LEETCODE_CSRF: process.env.LEETCODE_CSRF || 'fanum tax',
  LEETCODE_SESSION:
    process.env.LEETCODE_SESSION ||
    'bout that pricks face when he saw that gyat',
};

export default config;
